# Supabase bağlantısı — proje kuruldu (bkz. docs/GELISTIRME-PLANI.md, Aşama 2 tamam)
# Gerçek değerleri Supabase panelinden alın: Project Settings → API (projectfn projesi)
# Bu iki değer istemciye gönderilir (publishable/anon anahtardır, gizli değildir);
# yine de gerçek değerleri buraya değil yalnızca .env.local'e girin.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
